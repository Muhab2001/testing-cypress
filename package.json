{
  "name": "cypress_tst",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "cy:run": "cypress run",
    "test": "start-server-and-test start http://localhost:3030 cy:run",
    "config": "cypress open --config-file=cypress.config.js"
  },
  "dependencies": {
    "next": "12.1.6",
    "react": "18.1.0",
    "react-dom": "18.1.0"
  },
  "devDependencies": {
    "cypress": "^10.0.2",
    "start-server-and-test": "^1.14.0"
  }
}
